# Frontend (React + Vite)

Installar dependencias y ejecutar en desarrollo:

```bash
cd scaffolding/frontend
npm install
npm run dev
```

La app usa Leaflet para mapas y `socket.io-client` para WebSockets.
