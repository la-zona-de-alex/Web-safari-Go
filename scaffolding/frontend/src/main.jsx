import React from 'react'
import { createRoot } from 'react-dom/client'
import App from './App'
import './styles.css'

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)

// flag for debugging: indicates client mount completed
try { window.__APP_MOUNTED = true } catch(e) { /* ignore */ }
